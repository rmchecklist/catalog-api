spring:
  application:
    name: catalog-api
  datasource:
    url: ${DB_URL1:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${DB_USERNAME1:postgres.aksxbcingfgvaffrrnag}
    password: ${DB_PASSWORD1:ilanfoods@123}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: ${DB_POOL_MAX:5}
      minimum-idle: ${DB_POOL_MIN:1}
      idle-timeout: ${DB_IDLE_TIMEOUT_MS:60000}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  flyway:
    enabled: false
  mail:
    host: ${SMTP_HOST:email-smtp.us-east-1.amazonaws.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:AKIAU6GDWDBZCR4CRZGM}
    password: ${SMTP_PASSWORD:BJbLoQGGm72skCYT7uglulERSp79QeFn8stoO5m2i/fF}
    from: ${MAIL_FROM:admin@ilanfoods.com}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

server:
  port: ${PORT:8080}
  servlet:
    context-path: /catalog

logging:
  level:
    org.springframework: info

app:
  external-base-url: ${APP_EXTERNAL_BASE_URL:http://localhost:8080/catalog}
  ui-base-url: ${APP_UI_BASE_URL:http://localhost:4200}

supabase:
  project-reference: ${SUPABASE_PROJECT:aksxbcingfgvaffrrnag}
  jwt:
    secret: ${SUPABASE_JWT_SECRET:Ifh0Xf7sNQwg4YaF5+BuVph/+n9hAyLPOALZoERKGoPNCw82PDeCOZNW+BhVY76ALpnw9wgWO3YnTz20ZUfzKA==}
  s3:
    access-key-id: ${SUPABASE_ACCESS_KEY_ID:ecda7550a746a6efded3e4ab5ccab8a8}
    secret-access-key: ${SUPABASE_SECRET_ACCESS_KEY:6bfef46403a165c138bd54e2a18172a0c12e17fa1267423a14ded99005d0f7c8}
    region: ${SUPABASE_REGION:us-east-1}
    endpoint-url: ${SUPABASE_ENDPOINT_URL:https://aksxbcingfgvaffrrnag.storage.supabase.co/storage/v1/s3}
    bucket-name: ${SUPABASE_BUCKET:products}

quotes:
  to: ${QUOTE_TO:${MAIL_FROM:}}

--- # Postgres profile example

logging:
  level:
    org.springframework: info
